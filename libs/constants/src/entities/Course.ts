export enum StatusCourse {
  Pending = 1,
  Approved = 2,
  Rejected = 3,
}

export enum TypeQueryCourse {
  Manage = 'manage',
  Show = 'show',
}

export enum TypeCourse {
  NEW = 1,
  HOT = 2,
}

export enum ModeCourse {
  Free = 1,
  PayFee = 2,
}
